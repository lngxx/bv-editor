[package]
name = "third_party_extension"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false
description = "Example extension living outside the core crates, standing in for a real third-party user of the bv-editor extension API."

[dependencies]
bevy_app = { workspace = true }
bevy_log = { workspace = true }

[dev-dependencies]
bv_editor_test_utils = { workspace = true }
